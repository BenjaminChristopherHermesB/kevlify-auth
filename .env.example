# Server Configuration
PORT=3001                        # Backend server port
NODE_ENV=development             # development | production

# Session
SESSION_SECRET=change-this-to-random-string  # Session encryption key (generate using: openssl rand -base64 32)

# Database
DATABASE_PATH=./data/kevlify.db  # SQLite file location

# CORS (comma-separated origins for production)
CORS_ORIGINS=http://localhost:5173

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Logging
LOG_LEVEL=debug                  # debug | info | warn | error

# Client-side encryption (informational - actual encryption happens in browser)
# Users create their own encryption passwords locally
